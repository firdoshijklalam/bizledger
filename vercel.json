{
  "buildCommand": "prisma generate --schema prisma/schema.prisma && npx prisma migrate deploy --schema prisma/schema.prisma && next build",
  "installCommand": "npm install",
  "framework": "nextjs"
}
